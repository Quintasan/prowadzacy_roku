%h1 Listing fixtures

%table
  %thead
    %tr
      %th
      %th
      %th

  %tbody
    - fixtures.each do |fixture|
      %tr
        %td= link_to 'Show', admin_fixtures_path(fixture)
        %td= link_to 'Edit', edit_admin_fixture_path(fixture)
        %td= link_to 'Destroy', admin_fixture_path(fixture), :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Fixture', new_admin_fixture_path
